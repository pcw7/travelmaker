<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardns">
	<select id="getTotal" resultType="integer">
		select count(*) from board where del='n'
	</select>
	<select id="list" parameterType="map" resultType="board">
		select * from (select a.*, rowNum rn from (select * from board where del='n' order by bno desc
		) a) where rn between #{startRow} and #{endRow}
	</select>
	<select id="getMaxNum" resultType="integer">
		select nvl(max(bno), 0) + 1 from board
	</select>
	<select id="select" parameterType="integer" resultType="board">
		select * from board where bno=#{bno}
	</select>
	<insert id="insert" parameterType="board">
		insert into board values (#{bno}, #{title}, sysdate, sysdate, '집', #{content}, sysdate, 0, 'n', 0)
	</insert>
	<update id="updateReadCount" parameterType="integer">
		update board set readcount = readcount + 1 where bno=#{bno}
	</update>
	<update id="update" parameterType="board">
		update board set title=#{title}, content=#{content} where bno=#{bno} 
	</update>
	<update id="delete" parameterType="integer">
		update board set del='y' where bno=#{bno}
	</update>
</mapper>